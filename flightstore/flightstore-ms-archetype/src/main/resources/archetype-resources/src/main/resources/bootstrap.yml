server:
  port: 9180
  servlet:
    context-path: /${artifactId}
    
management:
  endpoint:
    refresh:
      enabled: true          
  endpoints:
    web:
      exposure:
        include: "*"
        
spring:
  application:
    name: ${artifactId}
  profiles:
    active: "mac"
  # For Kubernetes ConfigMaps  
  cloud:
    kubernetes:
      config:
        name: ${artifactId}
        namespace: myproject
        sources:
        - name: ${artifactId}
      reload:
        enabled: true
        mode: polling
        period: 10000