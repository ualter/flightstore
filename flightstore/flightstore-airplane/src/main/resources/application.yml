server:
  port: 9180
  servlet:
    context-path: /flightstore-airplane
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
    
spring:
  application:
    name: flightstore-airplane
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://${MYSQL_IP:#{'localhost'}}:${MYSQL_PORT:#{'3306'}}/fs-airplane?allowPublicKeyRetrieval=true&nullNamePatternMatchesAll=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&serverTimezone=UTC"
    username: "flightuser"
    password: ENC(4EiGHH8mTjJYkr4sxwwIVg==)
  jpa:
    hibernate:
        # none, validate, update, create, and create-drop
        ddl-auto: "validate"
  cache:
    redis:
      time-to-live: 120000 #two minutes of caching
  redis:
    host: "127.0.0.1"
    port: 6379
       

