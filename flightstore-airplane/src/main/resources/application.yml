server:
  port: 9180
  servlet:
    context-path: /flightstore-airplane
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
    
spring:
  application:
    name: flightstore-airplane
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://${MYSQL_IP:#{'localhost'}}:${MYSQL_PORT:#{'3306'}}/flightstore?allowPublicKeyRetrieval=true&nullNamePatternMatchesAll=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&serverTimezone=UTC"
    username: "flightuser"
    password: ENC(JS72xB5PFeAFXu4wTXO7FQ==)
  jpa:
    hibernate:
        # none, validate, update, create, and create-drop
        ddl-auto: "validate"

