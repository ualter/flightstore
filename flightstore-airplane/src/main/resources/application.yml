server:
  port: 9180
  servlet:
    context-path: /flightstore-airplane
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
    
spring:
  application:
    name: flightstore-airplane
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://${MYSQL_IP}:${MYSQL_PORT}/flightstore?allowPublicKeyRetrieval=true&nullNamePatternMatchesAll=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&serverTimezone=UTC"
    username: "flightuser"
    password: "takeoff"
  jpa:
    hibernate:
        ddl-auto: "create"
        #ddl-auto: "update"
  #mvc:
  #  view:
  #     prefix: "/views/"
  #     suffix: ".html"   
            
